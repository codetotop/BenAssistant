<resources>
    <!-- Android 12+ (API 31+) themed splash screen -->
    <style name="Base.Theme.BenAssistant" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.BenAssistant" parent="Base.Theme.BenAssistant">
        <item name="android:textColor">@color/black</item>

        <!-- Android 12+ splash screen -->
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <!-- Optional splash icon and background color -->
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>
</resources>
